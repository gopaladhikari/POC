[project]
name = "pandas-numpy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "gopaladhikari", email = "gopuadks@gmail.com" }]
requires-python = ">=3.14"
dependencies = ["pandas>=3.0.5"]

[project.scripts]
pandas = "pandas_numpy.pandas.main:main"
numpy = "pandas_numpy.numpy.main:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
